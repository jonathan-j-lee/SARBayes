// SARbayes
// $ dot network.dot -Tpng -o network.png; display network.png

digraph network {
    age -> status;
    hours -> status;
    temp_max -> status;
    temp_min -> status;
    wind_speed -> status;
    snow -> status;
    rain -> status;
}
